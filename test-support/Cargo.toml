[package]
name = "alix-test-support"
version = "0.0.0"
edition = "2024"
publish = false

# Deliberately no dependency on alix: an expectation computed from the code
# under test asserts nothing.
[dependencies]
# Projects the progress document down to which cards carry a schedule.
serde_json = "1"
