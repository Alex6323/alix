name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  workflow_call:
    inputs:
      release_gate:
        description: Skip informational-only jobs when gating a release tag
        required: false
        type: boolean
        default: false

# One run per branch/PR: cancel superseded runs, but never on main.
concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

env:
  # Warnings are errors in CI — this is the "clippy must be green" gate.
  # Local `make check` stays lenient; CI runs the same command, strictly.
  RUSTFLAGS: -Dwarnings

jobs:
  # Formatting uses the NIGHTLY toolchain on purpose: rustfmt.toml has
  # nightly-only options (see the Makefile). No compile, so it's its own fast job.
  fmt:
    name: rustfmt (nightly)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: nightly-2026-07-23
          components: rustfmt
      - run: make fmt-check

  # clippy + tests on stable — the exact `make check` you run locally.
  check:
    name: clippy + tests (stable)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      # `make check` verifies formatting first, and rustfmt.toml is nightly-only.
      # Installed before stable so stable remains the default toolchain.
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: nightly-2026-07-23
          components: rustfmt
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: 1.97.1
          components: clippy
      - uses: Swatinem/rust-cache@f0d9c3887740aee45f6153b24b3a6b815192ec16 # v2
      # `make check` includes pre-1-0-check, whose script runs ripgrep; the
      # runner image does not ship it.
      - run: sudo apt-get update && sudo apt-get install -y ripgrep
      # deps-check inspects `cargo tree --locked --offline`, which needs the
      # registry populated on a fresh runner.
      - run: cargo fetch
      - run: make check

  # The development orchestrator is a standalone Python project outside Cargo.
  # Its deterministic unit and type checks block every PR; the real-agent smoke
  # remains a separate, costed manual command.
  orchestrator:
    name: orchestrator (Python)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "0.11.16"
          python-version: "3.12"
          enable-cache: true
          cache-dependency-glob: orchestrator/uv.lock
      - run: uv run --frozen --project orchestrator python -m unittest discover -s orchestrator/tests
      - run: uv run --frozen --project orchestrator mypy --config-file orchestrator/pyproject.toml orchestrator/src orchestrator/tests

  # The persistence and path-sensitive suites on the other two supported
  # desktop platforms. Cross-compiling a Windows binary does not test Windows
  # filesystem semantics (rename-over-existing, the dir-fsync no-op), so these
  # must actually run there. Windows runs the lib suites only: the fake AI
  # backend used by the integration tests is a /bin/sh script (see
  # src/testutil.rs), so those are unix-gated. macOS runs everything.
  persistence-os:
    name: tests (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: macos-latest, args: "" }
          - { os: windows-latest, args: --lib }
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: 1.97.1
      - uses: Swatinem/rust-cache@f0d9c3887740aee45f6153b24b3a6b815192ec16 # v2
      - run: cargo test ${{ matrix.args }}

  # Cargo never parses the served JavaScript. Keep this separate and fast so
  # inline, standalone, and composed-module failures do not wait for browsers.
  web-lint:
    name: web JavaScript composition
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "20.18.0"
      - run: make lint-js unit-js

  # The lean core must keep compiling AI/server-free: the mobile-client contract.
  # This is the guard that stops a core module from quietly reaching into a gated one.
  core:
    name: lean core (no-default-features)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: 1.97.1
      - uses: Swatinem/rust-cache@f0d9c3887740aee45f6153b24b3a6b815192ec16 # v2
      - run: make build-core

  # The frb bridge crate (mobile/alix/rust) wraps the lean core for the mobile
  # app. Pure host Rust (no Android SDK, no Flutter), so it is cheap enough to
  # block every PR: this is what catches a lib API change silently breaking
  # the mobile bridge between emulator runs. The full on-device story is the
  # scheduled mobile build, not this job.
  bridge:
    name: mobile bridge (host tests)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: 1.97.1
      - uses: Swatinem/rust-cache@f0d9c3887740aee45f6153b24b3a6b815192ec16 # v2
        with:
          workspaces: mobile/alix/rust
      - run: cargo test --manifest-path mobile/alix/rust/Cargo.toml

  # The mobile suite against the real embedded core, no emulator: the Dart
  # unit + widget tests load the host dylib, then the full-app integration
  # test drives picker -> review -> grade in a headless Linux window and
  # asserts the store file. Blocking like e2e, and for the same reason: this
  # is the layer that catches a change silently breaking the mobile review
  # flow. Time is injected through the bridge, so nothing here sleeps; the
  # on-Android tier of the same suite belongs to the weekly mobile-drift
  # workflow.
  mobile-e2e:
    name: mobile e2e (linux)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2
        with:
          channel: stable
          flutter-version-file: mobile/alix/.fvmrc
          cache: true
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: 1.97.1
      - uses: Swatinem/rust-cache@f0d9c3887740aee45f6153b24b3a6b815192ec16 # v2
        with:
          workspaces: |
            .
            mobile/alix/rust
      - name: Linux desktop build dependencies
        run: sudo apt-get update && sudo apt-get install -y ninja-build libgtk-3-dev xvfb
      - run: cd mobile/alix && flutter analyze --fatal-infos
      - run: cargo build --release --manifest-path mobile/alix/rust/Cargo.toml
      - run: cd mobile/alix && flutter test
      - run: cd mobile/alix && xvfb-run -a flutter test integration_test -d linux

  # Browser-driven smoke over the real binary: a click must reach the server and
  # change the screen, with no uncaught page errors. This is the only layer that
  # catches a control which silently does nothing. Blocking, on purpose — an
  # advisory red here would decay into noise. What it does NOT cover, and why,
  # is written down in e2e/README.md.
  e2e:
    name: e2e (playwright)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: 1.97.1
      - uses: Swatinem/rust-cache@f0d9c3887740aee45f6153b24b3a6b815192ec16 # v2
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "20.18.0"
          cache: npm
          cache-dependency-path: e2e/package-lock.json
      # Playwright's `webServer` shells out to `cargo run`. A cold compile inside
      # its start timeout is a classic flake, so build first: the servers then
      # boot in about a second.
      - run: cargo build
      - name: Cache Playwright browsers
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('e2e/package-lock.json') }}
      - run: make e2e
      # A red run that only says "timeout" gets ignored, then disabled, then
      # deleted. The trace makes it actionable (config sets retain-on-failure).
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: failure()
        with:
          name: e2e-traces
          path: e2e/test-results/
          if-no-files-found: ignore

  # Coverage — informational (a flashlight, not a gate): it computes line
  # coverage, prints the summary, uploads the browsable HTML as an artifact, and
  # uploads lcov to Codecov (also informational — see codecov.yml). Nightly-only
  # toolchain: `make coverage`/`make coverage-lcov` both run under
  # `cargo +nightly llvm-cov` so the `#[cfg_attr(coverage_nightly, ...)]`
  # exclusions in src/ apply consistently to both reports.
  coverage:
    name: coverage
    # Release callers need every blocking CI job at the tag SHA. Coverage is
    # informational and remains part of normal branch and pull-request CI.
    if: ${{ !inputs.release_gate }}
    runs-on: ubuntu-latest
    env:
      RUSTFLAGS: "" # coverage instruments its own flags; it isn't the warnings gate
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: nightly-2026-07-23
          components: llvm-tools-preview
      - uses: taiki-e/install-action@37f7c5781271959fb65b6b35224e28652ff2b63d # v2
        with:
          tool: cargo-llvm-cov@0.8.5
          fallback: none
      - uses: Swatinem/rust-cache@f0d9c3887740aee45f6153b24b3a6b815192ec16 # v2
      - run: make coverage
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-html
          path: target/llvm-cov/html/
      # lcov output for Codecov (see Makefile's coverage-lcov). Informational
      # only — codecov.yml sets both statuses to informational and
      # fail_ci_if_error is false, so this never gates CI.
      - run: make coverage-lcov
      - uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov.info
          fail_ci_if_error: false

  # GFM corpus drift gate: the committed tools/gfm-harness baselines must
  # match the current parser, so a parser change can never land without its
  # corpus-wide effect re-measured and reviewed in the same commit. Scoped:
  # only lib or harness changes can move measurements, so pushes touching
  # neither skip the build entirely.
  gfm-baseline:
    name: gfm corpus baseline drift
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - name: Decide whether measured surfaces changed
        id: scope
        run: |
          base="${{ github.event.pull_request.base.sha || github.event.before }}"
          if [ -z "$base" ] || ! git cat-file -e "$base" 2>/dev/null; then
            # Unknown or unfetchable base (new branch, force push): fail open
            # into running the gate rather than silently skipping it.
            echo "run=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if git diff --name-only "$base" HEAD | grep -qE '^(src/|tools/gfm-harness/|Cargo\.toml|Cargo\.lock|Makefile)'; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        if: steps.scope.outputs.run == 'true'
        with:
          toolchain: 1.97.1
      - uses: Swatinem/rust-cache@f0d9c3887740aee45f6153b24b3a6b815192ec16 # v2
        if: steps.scope.outputs.run == 'true'
        with:
          workspaces: tools/gfm-harness
      - name: Regenerate baselines and require a clean diff
        if: steps.scope.outputs.run == 'true'
        run: |
          make gfm-measure
          git diff --exit-code -- tools/gfm-harness/baseline
