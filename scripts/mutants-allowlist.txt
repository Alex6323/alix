# Mutation-miss allowlist. scripts/mutants_allowed.py reconciles
# cargo-mutants misses against this file: a run stays red unless every
# miss matches an entry here. Entries document argued residuals; the
# mutants still run every sweep, so red means NEW information.
#
# Down from twenty-four groups. Alex ruled
# 2026-08-06 that the only admissible reason is "it really cannot be
# tested", so everything merely awkward was seamed and killed instead:
# the uuid mixer became a pure function of its seed (11 killed), the
# pairing host became a parameter (1), and a child's outcome separated
# from spawning it (4). What remains is genuinely untestable, by one of
# four proofs:
#
#   EQUIVALENT    output identical for every input, provably.
#   UNREACHABLE   the branch cannot be entered; kept as a loud guard.
#   UNOBSERVABLE  the value has no consumer on this path.
#   IO-LEAF       nothing but a syscall remains; the logic it feeds is
#                 tested through a seam.
#
# No entry may say "no test covers this yet". That is a missing test.
#
# Both survivor classes are reconciled here: a MISS (the tests passed
# under the mutant) and a TIMEOUT (the mutated tests hung). Timeouts need
# entries too, because cargo-mutants reports a timeout in preference to a
# miss, so one unargued timeout keeps a shard red no matter how well its
# misses are argued. Alex ruled 2026-08-10: a hang counts as detection
# ONLY where the mutant is proven NON-TERMINATING at that site. Every
# other hang stays red, because a timeout is evidence about the mutant,
# not about the tests. The 2026-08-09 nightly is why: of its four hangs,
# two were real, one mutant was already caught, and one was a plain miss
# that the hang hid (`deck_due`, which runs green in 16s).
#
# Every group states its CLASS, because "residual" must never be read as
# "impossible". Five classes, only the first two of which are unfixable:
#
#   EQUIVALENT   no test can distinguish the mutant: output is identical
#                for every input. Genuinely unkillable.
#   NON-TERMINATING  the only survivor class for a TIMEOUT. The mutated
#                expression is a loop's only advance, so the loop never
#                exits and hanging is the only outcome any budget can
#                produce. Proof is per SITE, never per operator: the same
#                substitution terminates wherever something else advances
#                the counter. cargo-mutants usually supplies the contrast
#                itself, as a sibling mutant on the same line that
#                terminates and is caught. Prefer deleting the manual
#                counter loop (an iterator expression has no such site) to
#                writing the entry: that is what closed both of the
#                2026-08-09 hangs, and it leaves nothing to argue.
#   DELIBERATE   unreachable code kept on purpose (defence in depth). The
#                mutant is the price of the safety net; killing it means
#                deleting the net.
#   DEAD-PATH    the mutated value has no consumer on this path, but the
#                code is shared with paths where it matters, so it cannot
#                be removed here.
#   NEEDS-SEAM   killable, but only by adding a testing seam to production
#                code (injectable randomness, an injectable host lookup, a
#                spawn indirection). NOT a wall: an open decision, and one
#                Alex can overturn at any time.
#
# Entry forms, both exactly as cargo-mutants prints the survivor:
#   <file>: <mutation description>          argued CLASS (any line)
#   <file>:<line>: <mutation description>   single argued site (rots
#                                           loudly via the stale note
#                                           when the line moves)
# Every entry group MUST carry its argument in the comment above it.
# An entry without an argument, or whose code has changed since the
# argument, does not belong here. Remove an entry the moment a
# legitimate law kills its mutant; the "matched nothing" note on a
# whole-tree sweep of its shard is the removal reminder.

# EQUIVALENT, provably. Both sites mask the target bit to zero before
# setting it: `(b[6] & 0x0f) | 0x40` and `(b[8] & 0x3f) | 0x80`. After the
# mask the bit is 0 for EVERY input, so OR and XOR give the same result
# and no test can distinguish them. The other eleven mixer mutants were
# killed by pinning uuid_from_seed's exact output for known seeds.
src/ask.rs: replace | with ^ in uuid_from_seed

# UNREACHABLE. parse_drafted_card rejects either empty side
# before this condition, so no single-empty state can reach it and
# the two operators are indistinguishable from any input.
src/ask.rs: replace || with && in parse_drafted_card

# IO-LEAF. After the announce seam, local_lan_ip is three lines of pure
# syscall (bind, connect, read the local address): no logic remains to
# test, and asserting it returns an address needs a live routing table.
# The decision it feeds IS tested, via announce_with taking the address
# as a value; the match-arm mutant there is killed.
src/cli/launch.rs: replace local_lan_ip -> Option<std::net::IpAddr> with None

# EQUIVALENT, provably. The line above already sets `dropped[index]`, and
# `quoted_run_end` starts its scan at `marker + 1` and only grows, so it
# never returns less than `index + 1`. The mutant widens the fill by the
# one element that is already `true`; every other element is untouched.
# Output is identical for every input. Line-pinned because the marker
# scan holds other arithmetic.
src/parser/sidecar.rs:39: replace + with * in without_notes

# EQUIVALENT. key_rank's only consumer is a sort_by_key; only pairwise
# order is observable. Ranks are canonical 0..=18, unknown 19, `for` 20,
# `id` 21; under `* 2` id becomes 38, and no key can rank inside (21, 38),
# so every pairwise comparison is unchanged for every input. Line-pinned to
# the `id` rank; the `for` rank immediately above is a different site.
src/parser/frontmatter.rs:387: replace + with * in key_rank

# EQUIVALENT, provably. `ground` reaches this comparison only through
# `hex_color`, so `ink` is always the luminance of an 8-bit RGB triple.
# Enumerating all 16,777,216 of them against the shipped `relative_luminance`
# and `contrast` yields zero exact ties; the closest is `#6c907a`, whose two
# ratios differ by 0.00000021507230041351. `>` and `>=` therefore select the
# same ground for every input the function can receive. Were a tie reachable,
# both grounds would by definition contrast identically, so the tie-break
# carries no legibility meaning worth pinning either. Line-pinned: the
# argument is about this one comparison, not about the function.
src/diagram.rs:222: replace > with >= in ground

# EQUIVALENT, provably, and by inspection rather than enumeration. The only
# inputs where `>` and `>=` differ are those with `a == b`, and there
# `(a, b)` and `(b, a)` are the same pair, so the quotient is identical. For
# every other pair the operators already agree, NaN included, where both are
# false and the `else` arm is taken either way. No input distinguishes them.
# Line-pinned: the argument is about this one comparison, so a `>` surviving
# anywhere else in the function must be argued on its own.
src/diagram.rs:247: replace > with >= in contrast

# EQUIVALENT. The quote scan reaches this guard with either a closing quote at
# `j < s.len()` or an unterminated value at `j == s.len()`; it cannot overshoot.
# The operators agree in the first case. In the second, the mutant increments
# `j` from len to len + 1 before assigning it to `i`, but the outer
# `i < s.len()` loop exits for both values and neither index is observed again.
# Line-pinned because the preceding three bounds in this function are distinct.
src/icon.rs:199: replace < with <= in strip_attrs

# EQUIVALENT. The only extra candidate the widened bound admits is
# `at == glyphs.len()`, and `raw` is `glyphs.get(at)` (src/inline.rs:617), so
# that iteration reads `None`, the `matches!` is false, the loop advances once
# more and stops. The check at line 673 then tests
# `raw(at) != Some(closer)`, which is true for an out-of-bounds `at` either
# way, so both versions return `None`. Line-pinned to the quoted-title closer
# scan; the angle-destination bound at line 633 is a different site.
src/inline.rs:668: replace < with <= in link_tail_end

# EQUIVALENT. Both callers enter with `chars[start] == '<'`: src/inline.rs:925
# sits below the `chars[index] != '<'` early-continue at :921, and :1268 tests
# it explicitly. The widened range therefore adds one candidate that is `'<'`,
# never the `'>'` the search wants, so `find` returns the same index. The body
# slice at line 1120 reads `chars[start + 1..close]` and the mutant does
# not touch it. Line-pinned: the argument is about this one range start.
src/inline.rs:1119: replace + with * in autolink_end

# EQUIVALENT. `start` is the index of the first line accepted as a
# frontmatter fence, a `# ` heading, or a `## ` heading, so `lines[start]`
# trims to something non-empty that never opens with three backticks
# (`is_frontmatter_fence` matches `---` plus whitespace only). Both mutants
# admit exactly one extra state, `end == start + 1`, where the loop inspects
# that recognized start line, takes the same `else { break }`, and hands
# `space_cards` the identical slice. Nothing can drive `end` below
# `start + 1`, since `end` begins at `lines.len()` and `start` indexes into
# it. Line-pinned: the argument is about this one loop bound.
# Classified by Codex from rotation shard 12; the proof re-read and both
# mutants re-run before landing.
src/generate.rs:540: replace > with >= in clean_output
src/generate.rs:540: replace + with * in clean_output

# NON-TERMINATING. `end /= 1` leaves `end` unchanged, and this statement is
# the branch's only advance: the loop condition and the line it inspects are
# then invariant, so every budget produces a hang. Not executed here on
# purpose, since running it wedges the suite; the proof is the site, not the
# operator. Line-pinned. Classified by Codex from rotation shard 12.
src/generate.rs:543: replace -= with /= in clean_output

# EQUIVALENT. `content_start` is set on the first source byte by every
# preceding non-whitespace arm, including the opening parenthesis that raises
# `depth`. Reaching a non-final `)` therefore calls `get_or_insert` on an
# already-populated option, so its argument is never observed. The operators
# change only that dead argument and cannot change the decoded source, authored
# range, or remaining tail for any input. Line-pinned to the nested-close arm.
src/parser/cloze.rs:159: replace - with + in unbracketed_src
src/parser/cloze.rs:159: replace - with / in unbracketed_src
