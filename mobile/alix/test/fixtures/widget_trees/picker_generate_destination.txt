Scaffold
  Material
    Column
      Padding(EdgeInsets(16.0, 8.0, 16.0, 12.0))
        Text("<tmp>")
      Divider
        SizedBox(width=null, height=1.0)
          Center
            Container
              Padding(EdgeInsets.zero)
                ConstrainedBox
                  Padding(EdgeInsets(0.0, 0.0, 0.0, 1.0))
                    ConstrainedBox
      Expanded
        ListView
          Padding(EdgeInsets.all(24.0))
            Text("No subfolders here. Choose this folder, or step back.")
      SafeArea
        Padding(EdgeInsets.zero)
          Padding(EdgeInsets.all(16.0))
            FilledButton(enabled=true)
              ConstrainedBox
                Material
                  CustomPaint
                    InkWell(tap=true, longPress=false)
                      Padding(EdgeInsets(24.0, 0.0, 24.0, 0.0))
                        Align
                          Text("Use this folder")
    ConstrainedBox
      AppBar
        Material
          Align
            SafeArea
              Padding(EdgeInsets.zero)
                ConstrainedBox
                  Center
                    IconButton(enabled=false, tooltip="")
                      IconButton(enabled=true, tooltip="Back") key=[<StandardComponentType.backButton>]
                        _IconButtonM3(enabled=true)
                          ConstrainedBox
                            Material
                              CustomPaint
                                Tooltip("Back")
                                  InkWell(tap=true, longPress=false)
                                    Padding(EdgeInsets.all(8.0))
                                      Align
                                        Icon(e092)
                                          SizedBox(width=24.0, height=24.0)
                                            Center
                Text("Decks folder")
    Stack
