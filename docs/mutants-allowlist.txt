# Mutation-miss allowlist. scripts/mutants_allowed.py reconciles
# cargo-mutants misses against this file: a run stays red unless every
# miss matches an entry here. Entries document argued residuals; the
# mutants still run every sweep, so red means NEW information.
#
# Current contents by class: 2 EQUIVALENT, 1 DELIBERATE, 2 DEAD-PATH,
# 19 NEEDS-SEAM. Nineteen of the twenty-four are therefore a standing
# DECISION, not a wall: injectable randomness, an injectable host
# lookup, and the spawn indirection {#profile-launch-test-fork-bomb}
# already owns would make them killable. That trade (production surface
# for test reach) is Alex's to make. Nothing here should be read as
# saying it cannot be done.
#
# Both survivor classes are reconciled here: a MISS (the tests passed
# under the mutant) and a TIMEOUT (the mutated tests hung). Timeouts need
# entries too, because cargo-mutants reports a timeout in preference to a
# miss, so one unargued timeout keeps a shard red no matter how well its
# misses are argued. Whether a hang counts as detection at all is Alex's
# open ruling; until he rules, no timeout entry is written here and the
# shards that have them stay red on purpose.
#
# Every group states its CLASS, because "residual" must never be read as
# "impossible". Four classes, only the first of which is truly unfixable:
#
#   EQUIVALENT   no test can distinguish the mutant: output is identical
#                for every input. Genuinely unkillable.
#   DELIBERATE   unreachable code kept on purpose (defence in depth). The
#                mutant is the price of the safety net; killing it means
#                deleting the net.
#   DEAD-PATH    the mutated value has no consumer on this path, but the
#                code is shared with paths where it matters, so it cannot
#                be removed here.
#   NEEDS-SEAM   killable, but only by adding a testing seam to production
#                code (injectable randomness, an injectable host lookup, a
#                spawn indirection). NOT a wall: an open decision, and one
#                Alex can overturn at any time.
#
# Entry forms, both exactly as cargo-mutants prints the survivor:
#   <file>: <mutation description>          argued CLASS (any line)
#   <file>:<line>: <mutation description>   single argued site (rots
#                                           loudly via the stale note
#                                           when the line moves)
# Every entry group MUST carry its argument in the comment above it.
# An entry without an argument, or whose code has changed since the
# argument, does not belong here. Remove an entry the moment a
# legitimate law kills its mutant; the "matched nothing" note on a
# whole-tree sweep of its shard is the removal reminder.

# EQUIVALENT, provably. Both sites mask the target bit to zero before
# setting it: `(b[6] & 0x0f) | 0x40` and `(b[8] & 0x3f) | 0x80`. After the
# mask the bit is 0 for EVERY input, so OR and XOR give the same result
# and no test can distinguish them. The other eleven mixer mutants were
# killed by pinning uuid_from_seed's exact output for known seeds.
src/ask.rs: replace | with ^ in random_uuid

# DELIBERATE. parse_drafted_card rejects either empty side
# before this condition, so no single-empty state can reach it and
# the two operators are indistinguishable from any input.
src/ask.rs: replace || with && in parse_drafted_card

# EQUIVALENT. AskConfig::default().model is already None,
# so deleting the explicit field from the helper's struct expression
# builds the identical value.
src/ask.rs: delete field model from struct AskConfig expression in tests::config

# EQUIVALENT. The mutated private counter is re-capped by the .take(remaining)
# that follows and reset on every edit; no observable hint output or
# state differs under the substitution.
src/answer.rs: replace - with + in TypingValidator::hint

# DEAD-PATH. synthesize_virtual stores this line number only on the Card, and
# WarmItem::from_card discards it before any output is generated; the
# value has no consumer on this path.
src/cli/deck.rs: replace + with - in augment_cmd
src/cli/deck.rs: replace + with * in augment_cmd

# IO-LEAF. After the announce seam, local_lan_ip is three lines of pure
# syscall (bind, connect, read the local address): no logic remains to
# test, and asserting it returns an address needs a live routing table.
# The decision it feeds IS tested, via announce_with taking the address
# as a value; the match-arm mutant there is killed.
src/cli/launch.rs: replace local_lan_ip -> Option<std::net::IpAddr> with None

# DEAD-PATH. synthesize_virtual stores this line number only on the Card, and
# WarmItem::from_card discards it before any output is generated; the
# value has no consumer on this path.
src/cli/deck.rs: replace + with - in augment_cmd
src/cli/deck.rs: replace + with * in augment_cmd

# IO-LEAF. After the announce seam, local_lan_ip is three lines of pure
# syscall (bind, connect, read the local address): no logic remains to
# test, and asserting it returns an address needs a live routing table.
# The decision it feeds IS tested, via announce_with taking the address
# as a value; the match-arm mutant there is killed.
src/cli/launch.rs: replace local_lan_ip -> Option<std::net::IpAddr> with None

# NEEDS-SEAM. Observable only after a real child spawn, which tests must never
# drive: launch_all's spawn re-enters the test binary and fork-bombs
# (the {#profile-launch-test-fork-bomb} roadmap item owns the fix).
# Killable only after that design decision lands.
src/cli/profile.rs: delete match arm Err(error) in launch_all
src/cli/profile.rs: replace match guard !status.success() with true in launch_all
src/cli/profile.rs: replace match guard !status.success() with false in launch_all
src/cli/profile.rs: delete ! in launch_all

# The table end_line init is observable only for a table with no rows
# and no directive comments, and such a table mints no container id;
# every other table overwrites the init before any use. Line-pinned
# because scan() holds many other additions.
src/parser/mod.rs:507: replace + with - in scan
src/parser/mod.rs:507: replace + with * in scan

# The differing case needs an empty anchor line at file start; anchor
# lines are non-empty content lines by construction. Line-pinned to
# the rel > 0 comparison.
src/stamp.rs:448: replace > with >= in line_terminator

# Same class as the UUID mixer: random input, no injection seam, and
# the output remains six valid, varying alphabet characters under the
# substitution.
src/token.rs: replace * with + in mint_row
