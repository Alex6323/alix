# Property-suite adjudication

Review only the remaining property-test failure and the implementer's attached
claim. Do not inspect implementation code.

Failure:
{failure_summary}

Implementer claim:
{test_bug_claim}

If the test contradicts the frozen specification at `{spec_path}`, correct only
that test. Otherwise leave it unchanged and explain why the claim is rejected.
Do not commit.
